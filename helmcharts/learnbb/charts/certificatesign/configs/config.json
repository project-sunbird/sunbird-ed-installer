{
    "issuers": {
      "default": {
        "publicKey": "{{ .Values.global.CERTIFICATESIGN_PUBLIC_KEY }}",
        "privateKey": "{{ .Values.global.CERTIFICATESIGN_PRIVATE_KEY }}",
        "signatureType": "{{ .Values.SIGNING_KEY_TYPE }}",
        "verificationMethod": "did:india"
      }
    }
  }