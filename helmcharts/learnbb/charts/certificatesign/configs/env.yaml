  PORT: "{{ .Values.PORT }}"
  QR_TYPE: "{{ .Values.QR_TYPE }}"
  CERTIFICATE_DOMAIN_URL: "https://{{ .Values.global.domain }}"
  CERTIFICATE_PUBLIC_KEY: "{{ .Values.global.CERTIFICATESIGN_PUBLIC_KEY }}"
  CERTIFICATE_PRIVATE_KEY: "{{ .Values.global.CERTIFICATESIGN_PRIVATE_KEY }}"
  SIGNING_KEY_TYPE: "{{ .Values.SIGNING_KEY_TYPE }}"
  CACHE_CONTEXT_URLS: "https://{{ .Values.global.object_storage_endpoint }}/schemas/v1_context.json, https://{{ .Values.global.object_storage_endpoint }}/schemas/sunbird_context.json, {{ .Values.global.domain }}/schema/credential_template.json"
  JAVA_OPTIONS: "{{ .Values.java_options }}"