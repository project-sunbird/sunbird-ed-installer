
nameOverride: "ml-reports-service"
fullnameOverride: "ml-reports-service"

replicaCount: 1

image:
  repository: sunbirded.azurecr.io/ml-reports-service
  tag: "release-7.0.0_RC16_25baa89_2461"
  pullPolicy: IfNotPresent
  pullSecrets: []

resources:
  requests:
    cpu: 100m
    memory: 100Mi
  limits:
    cpu: 1
    memory: 1G

service:
  type: ClusterIP
  ports:
    - name: ml-reports-service
      port: 3000
      targetPort: 3000

commonAnnotations:
  reloader.stakater.com/auto: "true"

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
        

ml_reports_access_basepath: "/opt/core/keycloak-public-keys/"
